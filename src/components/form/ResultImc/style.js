import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    boxResult: {
        // backgroundColor: "#fff",
    },
    Result: {
        color: "#fff",
        fontSize: 24,
        textAlign: "center",
        paddingTop: 10,
        paddingBottom: 10,
    },
});

export default styles;